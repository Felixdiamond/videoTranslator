# ── torch triplet ─────────────────────────────────────────────────────────────
# Install all three together from the same index URL to avoid ABI mismatches.
# CUDA 12.1:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# CPU-only:   pip install torch torchvision torchaudio
# Kaggle:     do NOT install these — see kaggle_run.ipynb Cell 2.
torch
torchvision
torchaudio

# ML / Transcription
transformers>=4.40.0
accelerate
faster-whisper>=1.2.1
whisperx>=3.8.2

# Audio
librosa
soundfile
pydub
noisereduce
pyrubberband>=0.3.0  # requires: apt-get install rubberband-cli
demucs>=4.0.1

# TTS
gtts
# MeloTTS: pip install git+https://github.com/Felixdiamond/MeloTTS.git
# Qwen3-TTS: pip install git+https://github.com/QwenLM/Qwen3-TTS.git

# Video
moviepy

# Web server
fastapi
uvicorn[standard]

# Utilities
tqdm
psutil
pyyaml
sentencepiece
sacremoses
numpy
setuptools<=81